#include <stdio.h>

/**
 * mul - a function that multiplies two integers
 *
 * @a: first integer
 * @b: second integer
 *
 * Return: the value of the multiplication of (a) and (b)
 */

int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}

